{
    "name": "architecture-workshop",
    "version": "1.0.0",
    "scripts": {
        "init": "cd ./mock-backend && npm i && cd ../app && npm i",
        "app": "cd app && npm run start",
        "backend": "cd mock-backend && npm run start",
        "set-version": "npm version $VERSION && cd ./mock-backend && npm version $VERSION && cd ../app && npm version $VERSION"
    },
    "devDependencies": {
        "start-server-webpack-plugin": "^2.2.5",
        "webpack-node-externals": "^2.5.2"
    }
}
